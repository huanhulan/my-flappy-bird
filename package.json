{
  "name": "myFlappyBird",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch webpack.config.js --exec \"webpack-dev-server --env.NODE_ENV=development\"",
    "build": "rm -rf dist && webpack --env.NODE_ENV=production --config webpack.config.js "
  },
  "keywords": [
    "canvas",
    "webworker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "css-loader": "^0.27.3",
    "html-webpack-plugin": "^2.26.0",
    "nodemon": "^1.11.0",
    "style-loader": "^0.13.2",
    "webpack": "^2.2.0-rc.7",
    "webpack-dev-server": "^2.3.0",
    "worker-loader": "^0.8.0"
  },
  "dependencies": {
    "worker-loader": "^0.8.0"
  }
}
